Tags:
  dp
  树状数组
  线段数
  除法逆元

Description:
给定一数组f, f(n-1),f(n-2)...f(0)
两种操作：
1. 修改数组任意一位置的值
2. 查询某一区间(i,j) 将次序列作为B进制数，转化为十进制输出，
   即，f(i)*B^(i-j) + f(i-1)*B^(i-1 -j) ...

Solution:
维护f(i)*B^i 的树状数组，
求和后再除以B^j即可.(除法取模可转化为乘以逆元取模)
逆元可用扩展gcd 或 欧拉定理
